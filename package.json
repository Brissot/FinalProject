{
    "dependencies": {
        "cfb.js": "^4.3.2",
        "ejs": "^5.0.1",
        "express": "^5.2.1",
        "mongodb": "^7.1.0"
    },

    "type": "module",

    "scripts": {
        "build": "npm install",
        "start": "node --env-file=.env app.js 8080",
        "dev": "nodemon --env-file=.env app.js 8080"
    },

    "devDependencies": {
        "@types/node": "^25.5.0"
    }
}
